# Terraform files
.terraform/
*.tfstate
*.tfstate.backup
plan.out
state.json

# Local configurations and overrides
terraform.tfvars
k8s/overrides-*.yaml
k8s/overrides-*.yaml.tmp

# Downloaded Helm Charts
# We ignore the extracted directories but keep the installer script
k8s/helm/apigee-*/

# Service Account Keys and TLS Certs generated by Terraform
k8s/credentials/*.json
k8s/credentials/*.pem

# Temporary/Scratch Files
multi-region.txt
*.tmp
.DS_Store
kubectl
